package highlevellangs.code.hw.jira.src.models.resolver;

import java.util.HashMap;
import java.util.Map;

public class Resolver implements InterfaceResolver {
    private Map<Integer, int[]> map = new HashMap<>();
}